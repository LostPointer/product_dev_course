# Неделя 17: Docker основы

## Цели недели
- Понять контейнеризацию и Docker
- Создавать Dockerfile
- Работать с Docker образами и контейнерами
- Запускать приложения в Docker

## Теоретическая часть

### 1. Что такое Docker?

**Docker** - платформа для разработки, доставки и запуска приложений в контейнерах.

**Контейнер** - изолированная среда выполнения, содержащая:
- Приложение
- Зависимости
- Библиотеки
- Конфигурацию

**Преимущества:**
- Консистентность (работает одинаково везде)
- Изоляция (контейнеры не влияют друг на друга)
- Портативность (легко переносить между окружениями)
- Эффективность (легче чем виртуальные машины)

### 2. Docker vs Virtual Machines

```
┌─────────────────────────────────────────────────┐
│            Virtual Machines                     │
├─────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐           │
│  │ App A  │  │ App B  │  │ App C  │           │
│  ├────────┤  ├────────┤  ├────────┤           │
│  │  Bins  │  │  Bins  │  │  Bins  │           │
│  ├────────┤  ├────────┤  ├────────┤           │
│  │Guest OS│  │Guest OS│  │Guest OS│           │
│  └────────┘  └────────┘  └────────┘           │
│  ┌─────────────────────────────────┐           │
│  │         Hypervisor              │           │
│  └─────────────────────────────────┘           │
│  ┌─────────────────────────────────┐           │
│  │         Host OS                 │           │
│  └─────────────────────────────────┘           │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│               Docker Containers                 │
├─────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐           │
│  │ App A  │  │ App B  │  │ App C  │           │
│  ├────────┤  ├────────┤  ├────────┤           │
│  │  Bins  │  │  Bins  │  │  Bins  │           │
│  └────────┘  └────────┘  └────────┘           │
│  ┌─────────────────────────────────┐           │
│  │      Docker Engine              │           │
│  └─────────────────────────────────┘           │
│  ┌─────────────────────────────────┐           │
│  │         Host OS                 │           │
│  └─────────────────────────────────┘           │
└─────────────────────────────────────────────────┘
```

### 3. Основные концепции

**Image (Образ):**
- Read-only шаблон
- Содержит все необходимое для запуска
- Создается из Dockerfile
- Хранится в registry (Docker Hub, etc)

**Container (Контейнер):**
- Running instance образа
- Изолированный процесс
- Может быть запущен, остановлен, удален
- Имеет свою файловую систему

**Dockerfile:**
- Текстовый файл с инструкциями
- Описывает как собрать образ
- Версионируется в Git

**Registry:**
- Хранилище образов
- Docker Hub (публичный)
- Private registries

### 4. Docker Commands

**Основные команды:**
```bash
# Работа с образами
docker build -t myapp:latest .      # Собрать образ
docker images                        # Список образов
docker pull python:3.11              # Скачать образ
docker push myrepo/myapp:latest      # Загрузить образ
docker rmi myapp:latest              # Удалить образ

# Работа с контейнерами
docker run myapp:latest              # Запустить контейнер
docker run -d myapp:latest           # Запустить в background
docker run -p 8000:8000 myapp        # С маппингом портов
docker ps                            # Список запущенных
docker ps -a                         # Все контейнеры
docker stop container_id             # Остановить
docker start container_id            # Запустить
docker restart container_id          # Перезапустить
docker rm container_id               # Удалить
docker logs container_id             # Логи
docker exec -it container_id bash    # Зайти в контейнер

# Очистка
docker system prune                  # Очистить неиспользуемое
docker volume prune                  # Очистить volumes
```

## Практическая часть

### Задание 1: Первый Dockerfile

Создайте Dockerfile для Python приложения:

```dockerfile
# Указываем базовый образ
FROM python:3.11-slim

# Метаданные
LABEL maintainer="your.email@example.com"
LABEL description="Todo API"

# Рабочая директория в контейнере
WORKDIR /app

# Копирование requirements
COPY requirements.txt .

# Установка зависимостей
RUN pip install --no-cache-dir -r requirements.txt

# Копирование приложения
COPY . .

# Expose порта (документация)
EXPOSE 8000

# Команда запуска
CMD ["python", "main.py"]
```

**Сборка и запуск:**
```bash
# Сборка образа
docker build -t todo-api:v1 .

# Запуск контейнера
docker run -d -p 8000:8000 --name todo-app todo-api:v1

# Проверка
curl http://localhost:8000/health

# Логи
docker logs todo-app

# Остановка
docker stop todo-app

# Удаление
docker rm todo-app
```

### Задание 2: Оптимизированный Dockerfile

**Проблемы первого Dockerfile:**
- Каждое изменение кода пересобирает все
- Большой размер образа
- Не оптимально кэширование layers

**Улучшенный Dockerfile:**
```dockerfile
FROM python:3.11-slim

# Установка системных зависимостей
RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Копируем только requirements сначала (для кэширования)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Теперь копируем остальное
COPY . .

# Создаем non-root пользователя
RUN useradd -m -u 1000 appuser && \
    chown -R appuser:appuser /app
USER appuser

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:8000/health')" || exit 1

EXPOSE 8000

CMD ["python", "main.py"]
```

### Задание 3: Multi-stage Build

Для production лучше использовать multi-stage builds:

```dockerfile
# Stage 1: Builder
FROM python:3.11-slim as builder

WORKDIR /app

# Установка build dependencies
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Установка Python зависимостей
COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# Stage 2: Runtime
FROM python:3.11-slim

WORKDIR /app

# Копируем установленные пакеты из builder
COPY --from=builder /root/.local /root/.local

# Копируем приложение
COPY . .

# Update PATH
ENV PATH=/root/.local/bin:$PATH

# Non-root user
RUN useradd -m -u 1000 appuser && \
    chown -R appuser:appuser /app
USER appuser

HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:8000/health')"

EXPOSE 8000

CMD ["python", "main.py"]
```

**Преимущества:**
- Меньший размер финального образа
- Не включает build tools
- Более безопасный

### Задание 4: Docker Compose intro

Создайте `docker-compose.yml` для запуска приложения с PostgreSQL:

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/tododb
      - DEBUG=false
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tododb
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
```

**Использование:**
```bash
# Запустить все сервисы
docker-compose up -d

# Логи
docker-compose logs -f

# Остановить
docker-compose down

# Остановить и удалить volumes
docker-compose down -v
```

## Дополнительные материалы

### Полезные ссылки
- [Docker Documentation](https://docs.docker.com/)
- [Dockerfile Best Practices](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)
- [Docker Compose](https://docs.docker.com/compose/)

### Инструменты
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)
- [Dive](https://github.com/wagoodman/dive) - анализ layers образа
- [Hadolint](https://github.com/hadolint/hadolint) - линтер для Dockerfile

## Вопросы для самопроверки

1. В чем разница между образом и контейнером?
2. Что такое Docker layer и как работает кэширование?
3. Зачем нужен multi-stage build?
4. Почему важно использовать non-root user?
5. Как правильно передавать секреты в контейнер?

## Следующая неделя

На следующей неделе изучим продвинутые возможности Docker: networks, volumes, optimization и deployment strategies.

---

**Удачи с контейнеризацией! 🐳**


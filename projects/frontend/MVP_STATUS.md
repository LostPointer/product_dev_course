# Frontend MVP Status (moved)

Документ переехал в `docs/experiment-tracking-status-and-roadmap.md` (раздел **Frontend (Experiment Portal) — статус для MVP**).

Этот файл оставлен как “редирект”, чтобы не ломать старые ссылки.


# Статус фронтенда для MVP

## Что уже сделано ✅

### 1. Базовая структура приложения
- ✅ React + TypeScript + Vite настроен
- ✅ React Router для навигации
- ✅ React Query для работы с API
- ✅ Axios клиент с interceptors для токенов
- ✅ Базовый Layout компонент

### 2. CRUD экспериментов (Experiments)
- ✅ **Список экспериментов** (`/experiments`)
  - Фильтрация по project_id, status
  - Поиск по названию/описанию
  - Пагинация
  - Отображение статусов, тегов, метаданных

- ✅ **Создание эксперимента** (`/experiments/new`)
  - Форма с полями: project_id, name, description, experiment_type, tags, metadata
  - Валидация JSON для metadata

- ✅ **Детали эксперимента** (`/experiments/:id`)
  - Просмотр всей информации
  - Редактирование (UI есть, но форма не реализована)
  - Удаление эксперимента
  - Список запусков эксперимента

### 3. CRUD запусков (Runs)
- ✅ **Список запусков** (компонент `RunsList`)
  - Отображение в таблице
  - Статусы, параметры, длительность

- ✅ **Детали запуска** (`/runs/:id`)
  - Просмотр информации о запуске
  - Завершение запуска (complete)
  - Пометить как ошибка (fail)
  - Отображение параметров и метаданных

### 4. API клиент
- ✅ Базовый API клиент (`src/api/client.ts`)
- ✅ Методы для experiments: list, get, create, update, delete, search
- ✅ Методы для runs: list, get, create, update, complete, fail
- ✅ Автоматическое добавление токена из localStorage
- ✅ Обработка 401 ошибок (редирект на /login)

### 5. Типы TypeScript
- ✅ Полный набор типов в `src/types/index.ts`:
  - Experiment, Run
  - ExperimentCreate, ExperimentUpdate
  - RunCreate, RunUpdate
  - Responses для списков

---

## Что нужно сделать для MVP ❌

### 1. Аутентификация (Auth) ✅ ВЫПОЛНЕНО
**Требование MVP:** Auth должен работать

**Что реализовано:**
- ✅ Страница входа (`/login`)
- ✅ Интеграция с Auth Proxy (BFF)
- ✅ Обработка refresh токенов через HttpOnly cookies
- ✅ Защита роутов (ProtectedRoute компонент)
- ✅ Отображение пользователя в Layout
- ✅ Кнопка выхода

**API endpoints (Auth Proxy):**
- ✅ `POST /auth/login` - вход
- ✅ `POST /auth/refresh` - обновление токена
- ✅ `POST /auth/logout` - выход
- ✅ `GET /auth/me` - профиль пользователя

---

### 2. Управление датчиками (Sensors) ✅ ВЫПОЛНЕНО
**Требование MVP:** Список датчиков

**Что реализовано:**
- ✅ Страница списка датчиков (`/sensors`)
- ✅ Страница создания/регистрации датчика (`/sensors/new`)
- ✅ Страница деталей датчика (`/sensors/:id`)
- ✅ API методы для работы с датчиками
- ✅ Типы для Sensor
- ✅ Фильтрация по project_id и status
- ✅ Пагинация
- ✅ Управление токенами датчиков (rotate token)
- ✅ Тесты для всех компонентов и API методов

**API endpoints (Experiment Service):**
- ✅ `GET /api/v1/sensors?project_id=...`
- ✅ `POST /api/v1/sensors`
- ✅ `GET /api/v1/sensors/{id}`
- ✅ `PATCH /api/v1/sensors/{id}`
- ✅ `DELETE /api/v1/sensors/{id}`
- ✅ `POST /api/v1/sensors/{id}/rotate-token`

---

### 3. Capture Sessions (Старт/Стоп отсчёта) 🔴 КРИТИЧНО
**Требование MVP:** Кнопка старт/стоп отсчёта с сохранением сырых данных

**Что отсутствует:**
- ❌ UI для управления capture sessions
- ❌ Кнопки "Старт отсчёта" / "Стоп отсчёта"
- ❌ Отображение списка capture sessions для run
- ❌ API методы для capture sessions
- ❌ Типы для CaptureSession

**Что нужно:**
1. Добавить типы в `src/types/index.ts`:
   ```typescript
   interface CaptureSession {
     id: string
     run_id: string
     project_id: string
     ordinal_number: number
     status: 'draft' | 'running' | 'succeeded' | 'failed' | 'archived'
     started_at?: string
     stopped_at?: string
     notes?: string
     // ...
   }
   ```

2. Добавить API методы в `src/api/client.ts`:
   - `captureSessionsApi.list(runId)`
   - `captureSessionsApi.create(runId, data)`
   - `captureSessionsApi.stop(runId, sessionId)`
   - `captureSessionsApi.delete(runId, sessionId)`

3. Обновить страницу `RunDetail.tsx`:
   - Добавить кнопку "Старт отсчёта" (если run в статусе `running`)
   - Показывать активную capture session
   - Кнопка "Стоп отсчёта" для активной сессии
   - Список всех capture sessions для run

4. Обновить страницу `ExperimentDetail.tsx`:
   - В списке runs показывать статус capture session

**API endpoints (Experiment Service):**
- `GET /api/v1/runs/{run_id}/capture-sessions`
- `POST /api/v1/runs/{run_id}/capture-sessions`
- `POST /api/v1/runs/{run_id}/capture-sessions/{session_id}/stop`
- `DELETE /api/v1/runs/{run_id}/capture-sessions/{session_id}`

---

### 4. Создание Run из UI 🟡 ВАЖНО
**Текущее состояние:** В `ExperimentDetail.tsx` есть кнопка "Новый запуск", но она только показывает alert

**Что нужно:**
1. Создать модальное окно или страницу для создания run
2. Форма с полями:
   - name (обязательно)
   - parameters (JSON или форма)
   - notes (опционально)
   - metadata (опционально, JSON)
3. После создания - перейти на страницу деталей run

**API endpoint:**
- `POST /api/v1/experiments/{experiment_id}/runs`

---

### 5. Telemetry Ingest (REST) 🟡 ВАЖНО
**Требование MVP:** Telemetry Ingest (REST)

**Примечание:** Это в основном backend функционал, но может понадобиться:
- Тестовая отправка телеметрии из UI (для проверки датчика)
- Мониторинг статуса приёма данных

**Что может понадобиться:**
1. Кнопка "Тестовая отправка" на странице датчика
2. Отображение последнего времени получения данных от датчика
3. Статус датчика (online/offline)

**API endpoint:**
- `POST /api/v1/telemetry` (используется датчиками, не UI напрямую)

---

### 6. Улучшения существующего функционала 🟢 ЖЕЛАТЕЛЬНО

#### 6.1 Редактирование эксперимента
- Сейчас есть кнопка "Редактировать", но форма не реализована
- Нужно добавить форму редактирования (можно модальное окно)

#### 6.2 Улучшение UI/UX
- Добавить loading states везде
- Улучшить обработку ошибок
- Добавить toast notifications для успешных операций
- Валидация форм на клиенте

#### 6.3 Навигация
- Добавить breadcrumbs
- Улучшить Layout (добавить меню пользователя, logout)

---

## Приоритеты для MVP

### Критично (обязательно для MVP):
1. ✅ CRUD экспериментов - **ГОТОВО**
2. ✅ CRUD запусков - **ГОТОВО**
3. ✅ **Аутентификация** - **ГОТОВО**
4. ✅ **Список датчиков** - **ГОТОВО**
5. ❌ **Capture Sessions (старт/стоп)** - нужно сделать

### Важно (желательно для MVP):
6. ❌ Создание Run из UI
7. ❌ Тестовая отправка телеметрии (для проверки датчиков)

### Можно отложить:
8. Редактирование эксперимента
9. Улучшения UI/UX
10. WebSocket/SSE стриминг (это для Beta, не MVP)

---

## Технические детали

### Зависимости
Все необходимые зависимости уже установлены:
- `@tanstack/react-query` - для работы с API
- `axios` - HTTP клиент
- `react-router-dom` - роутинг
- `date-fns` - форматирование дат

### Структура файлов для новых фич

```
src/
├── pages/
│   ├── Login.tsx              # НОВОЕ - страница входа
│   ├── SensorsList.tsx        # НОВОЕ - список датчиков
│   ├── SensorDetail.tsx       # НОВОЕ - детали датчика
│   ├── CreateSensor.tsx       # НОВОЕ - создание датчика
│   └── CreateRun.tsx          # НОВОЕ - создание run (модалка)
├── components/
│   ├── CaptureSessionControl.tsx  # НОВОЕ - управление capture session
│   └── CaptureSessionsList.tsx    # НОВОЕ - список capture sessions
├── api/
│   └── client.ts              # ДОПОЛНИТЬ - методы для sensors, capture sessions
└── types/
    └── index.ts               # ДОПОЛНИТЬ - типы для Sensor, CaptureSession
```

---

## Следующие шаги

1. **Начать с аутентификации** - это блокирует остальное
2. **Добавить управление датчиками** - критично для MVP
3. **Реализовать capture sessions** - основная фича MVP
4. **Добавить создание Run из UI** - улучшит UX
5. **Тестирование и полировка**


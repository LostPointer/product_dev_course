# Experiment Service Environment Configuration
# Для локальной разработки используйте localhost
# Для Docker Compose используйте имена сервисов (см. env.docker.example в корне проекта)

ENV=development
APP_NAME=experiment-service
HOST=0.0.0.0
PORT=8002

# Database Configuration
# Локально: postgresql://postgres:postgres@localhost:5432/experiment_db
# Docker: postgresql://postgres:postgres@postgres:5432/experiment_db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/experiment_db
DB_POOL_SIZE=20

# Auth Service (опционально, можно использовать заглушки через заголовки)
# Локально: http://localhost:8001/api/v1
# Docker: http://auth-service:8001/api/v1
AUTH_SERVICE_URL=http://localhost:8001/api/v1

# Message Broker (опционально, не используется активно)
# Локально: amqp://guest:guest@localhost:5672/
# Docker: amqp://guest:guest@rabbitmq:5672/
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Telemetry Broker (опционально, не используется активно)
# Локально: redis://localhost:6379/0
# Docker: redis://redis:6379/0
TELEMETRY_BROKER_URL=redis://localhost:6379/0

# OpenTelemetry (опционально)
# Локально: http://localhost:4318
# Docker: http://otel-collector:4318
OTEL_EXPORTER_ENDPOINT=http://localhost:4318

# CORS Configuration
# Comma-separated list of allowed origins
# Локально: http://localhost:3000,http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080


